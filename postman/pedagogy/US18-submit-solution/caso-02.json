{
  "description": "US18 — Caso 02: recurso con acepta_resoluciones=false rechaza la resolución.",
  "scenario": "Dado que el recurso no acepta resoluciones, cuando el estudiante intenta enviar una, entonces el sistema responde 403 Forbidden.",
  "request": {
    "method": "POST",
    "url": "{{api_v1}}/resources/{{resource_id}}/solutions",
    "headers": {
      "Authorization": "Bearer {{student_token}}",
      "Content-Type": "application/json"
    },
    "body": {
      "fileUrl": "{{file_url}}"
    }
  },
  "expected_response": {
    "status": 403
  }
}

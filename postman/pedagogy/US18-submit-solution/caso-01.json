{
  "description": "US18 — Caso 01: resolución enviada correctamente con fileUrl.",
  "scenario": "Dado que el recurso acepta resoluciones y no existe resolución previa, cuando el estudiante envía fileUrl válido, entonces el sistema registra la solución con status SUBMITTED.",
  "request": {
    "method": "POST",
    "url": "{{api_v1}}/resources/{{resource_id}}/solutions",
    "headers": {
      "Authorization": "Bearer {{student_token}}",
      "Content-Type": "application/json"
    },
    "body": {
      "fileUrl": "{{file_url}}"
    }
  },
  "expected_response": {
    "status": 201,
    "body": {
      "id": "<uuid>",
      "resourceId": "{{resource_id}}",
      "studentId": "<uuid>",
      "fileUrl": "{{file_url}}",
      "content": null,
      "status": "SUBMITTED",
      "submittedAt": "<datetime>"
    }
  }
}

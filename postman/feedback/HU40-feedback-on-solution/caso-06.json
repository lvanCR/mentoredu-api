{
  "description": "F1.2 — GET /feedback/given: docente consulta su historial de feedback emitido",
  "scenario": "Dado que el docente autenticado tiene entradas de feedback registradas, cuando consulta GET /feedback/given, entonces el sistema devuelve la lista ordenada por fecha descendente.",
  "request": {
    "method": "GET",
    "url": "{{api_v1}}/feedback/given",
    "headers": {
      "Authorization": "Bearer {{teacher_token}}"
    }
  },
  "expected_response": {
    "status": 200,
    "body": [
      {
        "id": "{{uuid}}",
        "authorUserId": "{{teacher_id}}",
        "targetUserId": "{{student_id}}"
      }
    ]
  }
}

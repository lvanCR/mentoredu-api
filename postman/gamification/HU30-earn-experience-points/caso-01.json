{
  "name": "MentorEduGamificationHU30-GetPointsGET",
  "description": "Caso 01 — Exitoso: el sistema registró XP tras una acción recompensada (responder hilo = +5 XP). Verificar mediante GET /points. Prerrequisito: haber ejecutado POST /threads/{id}/answers con el usuario.",
  "request": {
    "method": "GET",
    "url": "{{api_v1}}/gamification/users/{{userId}}/points",
    "header": [
      { "key": "Authorization", "value": "Bearer {{access_token}}" }
    ]
  },
  "expectedResponse": {
    "status": 200,
    "body": {
      "userId": "{{userId}}",
      "totalPoints": 5
    }
  }
}

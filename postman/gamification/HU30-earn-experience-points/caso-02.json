{
  "name": "MentorEduGamificationHU30-GetPointsGET",
  "description": "Caso 02 — Deduplicación: la misma acción fuente (mismo sourceType + sourceId) no genera puntos duplicados (RN-34). Consultar XP dos veces devuelve el mismo total.",
  "request": {
    "method": "GET",
    "url": "{{api_v1}}/gamification/users/{{userId}}/points",
    "header": [
      { "key": "Authorization", "value": "Bearer {{access_token}}" }
    ]
  },
  "expectedResponse": {
    "status": 200,
    "note": "El total de puntos NO debe haber incrementado respecto a la consulta anterior.",
    "body": {
      "userId": "{{userId}}",
      "totalPoints": 5
    }
  }
}

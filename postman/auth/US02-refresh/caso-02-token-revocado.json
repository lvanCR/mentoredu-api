{
  "escenario": "Caso 2 — Refresh token revocado",
  "gherkin": "Dado que mi refresh token ha sido revocado (ej. al cerrar sesión), cuando intento renovar el access token, entonces el sistema rechaza la solicitud.",
  "request": {
    "method": "POST",
    "url": "{{base_url}}/{{api_v1}}/auth/refresh",
    "headers": {
      "Content-Type": "application/json"
    },
    "body": {
      "refreshToken": "<refresh_token_revocado>"
    }
  },
  "expected_response": {
    "status": 401,
    "notas": "El sistema responde 401 Unauthorized. El token tiene 'revokedAt != null' en la tabla 'sessions'."
  }
}

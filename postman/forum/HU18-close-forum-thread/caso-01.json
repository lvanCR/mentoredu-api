{
  "name": "MentorEduForumHU18-CerrarHiloPATCH",
  "description": "Escenario exitoso: el autor cierra su propio hilo abierto. Espera 200 con el hilo en estado CLOSED.",
  "request": {
    "method": "PATCH",
    "url": "{{api_v1}}/threads/{{thread_id}}/close",
    "header": [
      {
        "key": "Authorization",
        "value": "Bearer {{access_token}}"
      }
    ]
  },
  "response": {
    "status": 200,
    "description": "Hilo cerrado exitosamente. Devuelve el ThreadResponse con status='CLOSED'."
  }
}

{
  "name": "MentorEduForumHU29-SelfFollowPOST",
  "description": "Caso 03 — Intentar seguirse a sí mismo. Usar el UUID del propio usuario autenticado como targetUserId. Espera 400 Bad Request.",
  "request": {
    "method": "POST",
    "url": "{{api_v1}}/users/{{self_user_id}}/follow",
    "header": [
      {
        "key": "Authorization",
        "value": "Bearer {{access_token}}"
      }
    ]
  },
  "expectedResponse": {
    "status": 400,
    "body": {
      "error": "Bad Request",
      "message": "No puedes seguirte a ti mismo"
    }
  }
}

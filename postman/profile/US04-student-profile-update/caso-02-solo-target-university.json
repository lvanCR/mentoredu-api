{
  "scenario": "Alt exitoso — Solo targetUniversityId enviado; demás campos conservan su valor",
  "gherkin": "Dado que ingreso una universidad válida por UUID, cuando confirmo la edición, entonces el sistema actualiza el dato sin modificar los demás campos.",
  "request": {
    "method": "PATCH",
    "url": "{{api_v1}}/profiles/student/me",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {{access_token}}"
    },
    "body": {
      "targetUniversityId": "{{university_id}}"
    }
  },
  "expected_response": {
    "status": 200,
    "body": {
      "profileId": "<uuid>",
      "targetUniversityId": "{{university_id}}"
    }
  },
  "note": "Todos los campos de actualización son opcionales en PATCH. Solo se modifican los enviados."
}

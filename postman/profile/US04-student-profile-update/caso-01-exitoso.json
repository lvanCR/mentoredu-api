{
  "scenario": "Exitoso — Perfil de estudiante existe, targetUniversityId actualizado",
  "gherkin": "Dado que mi perfil de estudiante existe, cuando cambio la universidad objetivo con un UUID válido del catálogo, entonces el sistema guarda la actualización.",
  "request": {
    "method": "PATCH",
    "url": "{{api_v1}}/profiles/student/me",
    "headers": {
      "Content-Type": "application/json",
      "Authorization": "Bearer {{access_token}}"
    },
    "body": {
      "targetUniversityId": "{{university_id}}"
    }
  },
  "expected_response": {
    "status": 200,
    "body": {
      "profileId": "<uuid>",
      "targetUniversityId": "{{university_id}}",
      "gradeLevel": "<valor previo sin cambios>",
      "schoolName": "<valor previo sin cambios>",
      "targetAreaId": "<valor previo sin cambios>",
      "targetCareerId": "<valor previo sin cambios>",
      "studyShift": "<valor previo sin cambios>"
    }
  },
  "note": "targetUniversityId debe ser un UUID válido del catálogo (seed V9). Usar {{university_id}} del entorno."
}
